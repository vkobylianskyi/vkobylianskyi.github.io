import { Box, styled } from "@mui/material";

export const CopyWrapper = styled(Box)`
    padding: 40px;
    margin-bottom: 40px;
    background-color: #262626;
    margin-top: 40px;
    border-radius: 5px;
    gap: 16px;
    flex-wrap: nowrap;
`;
